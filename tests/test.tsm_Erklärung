Es gibt einen Keyword-Speicher, welcher das letzte ausgeführte
KW beinhaltet

[]======================================[ Befehle ]==========================================[]

rep           =   [anz] [funktion] rep                = repeat funktion (anz-1) mal
out           =   [element] out                       = element ausgeben
outl          =   [element] outl                      = element ausgeben + neue zeile
swap          =   [e1] [e2] swap                      = e1 und e2 werden vertauscht
func          =   func [name]: ... end func [name]    = eine funktion name wird erstellt
if            =   [e1] [op] [e2] if: ... end if       = if (e1 op e2) { ... }
<funktion>    =   <funktion>                          = funktion wird auf den stack gelegt
run           =   [funktion] run                      = funktion wird ausgeführt
[]            =   [...]                               = Wird als Code behandelt, aber auf Befehl ausgeführt
set           =   [variable] [wert] set               = Variable wird auf Wert gesetzt

[]=================================[ BuildInVariablen ]======================================[]

$top          = oberstes Element vom Stack
$stack_index  = welcher Stack ist ausgewählt? (1 bis 10)
